/* @flow */

/* eslint-disable no-unused-vars */

type object = { [key: string]: any }

type PComponent = object

type SComponent = object

type TState = object

type TSocket = object

type TInputChangeEvent = {
  target: object
}
